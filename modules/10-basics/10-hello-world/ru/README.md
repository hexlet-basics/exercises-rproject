Изучать язык программирования, по традиции, начинают с программы 'Hello, World!'.

```text
Hello, World!
```

В языке R эта программа будет выглядеть так:

```r
cat("Hello, World!\n")
```

Текст `Hello, World!` появится на экране благодаря функции `cat`.
